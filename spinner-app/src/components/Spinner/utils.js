// export const randomColor = (previousColor) => {
//     const pastelColors = ["#FFC0CB", "#FFEB99", "#99FF99", "#99CCFF", "#FF99CC"];
//     const availableColors = pastelColors.filter((color) => color !== previousColor);
//     return availableColors[Math.floor(Math.random() * availableColors.length)];
//   };
  
  // export const randomDuration = () => Math.random() * 5 + 2;
  